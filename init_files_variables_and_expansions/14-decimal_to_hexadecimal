#!/bin/bash

# Check if DECIMAL is set and is a valid number
if [[ -z "$DECIMAL" ]] || ! [[ "$DECIMAL" =~ ^[0-9]+$ ]]; then
  echo "Error: DECIMAL environment variable is not set or is not a valid number."
  exit 1
fi

# Convert DECIMAL to hexadecimal and display it
printf "%x\n" "$DECIMAL"
